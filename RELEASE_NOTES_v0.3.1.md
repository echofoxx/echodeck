# EchoDeck v0.3.1

EchoDeck v0.3.1 is a small UI/version-label fix release.

## Fixes

- Fixes stale Now Playing eyebrow label that still showed `v0.2.6 Mobile UX`.
- Adds a dynamic `appVersionLabel` binding in the renderer.
- Updates package version and `VERSION.txt` to `0.3.1`.
- Keeps the v0.3.0 iOS Capacitor scaffold intact.
- Keeps desktop build workflow intact.

## Expected UI label

The Now Playing header should now show:

`v0.3.1 iOS Scaffold`

# Apply EchoDeck v0.2.7 Studio White Patch

1. Extract this patch over your existing EchoDeck project folder.
2. Run `npm install`.
3. Start the app with `npm start`.
4. Open Settings > Themes and choose **Studio White**.
5. Build Windows installers with `npm run package:win`.
